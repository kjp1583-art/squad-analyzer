# 작업 규칙 — squad-analyzer / squad-naejeon-bot

## 🗞 패치노트는 기능과 같이 나간다 (2026-09-15 사장님 지시)
> "브장신키우기 패치노트가 너무 안 발송되는데 / 클랜원들이 뭐가 바뀌는지 하나도 인지를 못하고 있어"

봇은 10분마다 조용히 자기를 갱신한다. 그래서 **공지를 따로 넣지 않으면 아무도 모른 채 지나간다.**

**클랜원 눈에 보이는 변화를 배포할 때는 `data/patch_notes.json` 에 항목을 같이 넣는다.**
- 새 콘텐츠·아이템·명령어, 확률/수치 조정, 눈에 띄는 버그 수정 → **넣는다**
- 내부 리팩터링, 로그, 테스트, 성능 → 넣지 않는다

### 형식
```json
{ "id": "2026-09-15-무엇",   // 한 번 배포하면 절대 바꾸지 않는다(봇의 '공지함' 기록 키)
  "date": "2026-09-15",
  "tag": "신규|개편|변경|밸런스|수정",
  "title": "🍣 한 줄 제목",
  "how": "채널에 아카보시",   // 쓰는 법(없으면 "")
  "lines": ["클랜원이 읽는 말로. 커밋 메시지 말고.", "**굵게** 와 `코드` 만 먹는다."] }
```
새 항목은 `notes` **배열 맨 앞**(최신 우선). 날짜 내림차순을 유지한다.

### 이게 어떻게 쓰이나 — 출처는 이 파일 하나
- **웹**: `index.html` 의 브장신 키우기 탭 → 🗞 패치노트 카드(`loadBrjPatch` / `brjPatchHtml`)
- **봇**: 부팅 뒤 `_brj_patch_announce()` 가 raw.githubusercontent 로 읽어 **아직 공지 안 한 것만**
  `#브장신키우기` 에 올리고 최신 것을 **고정(pin)** 한다. 공지한 id 는 `brj_patch_seen.json` 에 남는다
  (봇이 10분마다 재시작하므로 메모리에 두면 무한 반복된다).
- 클랜원은 채널에 `패치노트` 라고 쳐서 언제든 다시 볼 수 있다.

둘이 같은 파일을 보므로 **웹과 디스코드가 어긋날 일이 없다.** 한쪽만 고치지 말 것.

## 🧪 테스트할 땐 장비를 끼워라 (2026-09-16 사고)
> "레이드 공격이 항상 18/20 에서 멈춘다" — 원인 찾는 데 세 번 헛짚었다.

`_name_swap`(🪪 명패)이 `g["me"]` 만 갈아끼우고 `g["lanes"]`(밴픽 라인표)를 안 고쳐
결산에서 `KeyError` 가 났다. **명패를 낀 사람은 100% 터졌다.**

못 잡은 이유는 하나다 — **재현 스크립트의 테스트 캐릭터에 장비를 하나도 안 끼웠다.**
300판을 돌려도 장비 효과 코드는 한 줄도 실행되지 않았다.

전투 쪽(팀뽑·밴픽·결산·레이드·야생)을 건드렸으면 **반드시**:
- `r["gear"]` / `r["gear_lv"]` / `r["gear_um"]` 를 채워서 돌린다
- **장비 전 종목 × 보스 3종(sr·ddo·yj) × 엄/비엄** 을 한 번씩은 통과시킨다
- 조건부 경로(레벨·칭호·회차·연패)도 같은 눈으로 본다 — 평범한 캐릭터는 안 밟는다

그리고 **뭘 확정해 두고 원본만 바꾸는 코드**를 의심하라. `g["lanes"]` 는 `_bp_init` 이
확정하고, `g["me"]` 를 나중에 바꾸는 쪽이 라인표를 같이 안 고치면 조용히 어긋난다.
`g["hp"]["st"]`(`_hp_init` 캐시)도 같은 성질이다.

## 배포
- 두 저장소 모두 `claude/project-cloud-migration-0qtorl` 에서 작업 → main 으로 fast-forward
- 웹을 고쳤으면 `index.html` 의 `BUILD_ID` 를 올린다(안 올리면 캐시 때문에 안 바뀐 것처럼 보인다)
- 봇은 main 을 10분 내 스스로 받아 재시작한다

## 안 되는 것
- 커밋 메시지·PR 본문·코드 주석에 모델 이름을 적지 않는다
- 토큰·API 키는 코드에 넣지 않는다(GitHub secrets / 봇 환경변수)
- 웹에 생년 원값을 노출하지 않는다(연령대만)
