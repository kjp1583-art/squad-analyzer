# HANDOFF — 견제 압력(Ban Pressure) 지표를 squad.gg 웹에 적용

작성: 2026-07-21 / 작성 세션: AI선생 주간평 개편 작업
목적: **견제 압력**이라는 신규 지표를 웹(index.html) 프로필에 노출한다.

---

## 1. 이 지표가 뭔가

**"상대 팀이 나 때문에 내 주력 챔프를 밴하는 정도."**

기존 지표(승률·KDA·시야·킬관여)는 전부 **본인이 op.gg 보면 아는 것**이다.
견제 압력은 다르다 — 계산하려면 **내가 낀 모든 판의 상대 팀 밴을 전부 봐야** 해서
개인은 절대 알 수 없다. 그래서 프로필에 넣을 값어치가 있다.

예시(실측):

| 선수 | 챔프 | 내가 낀 판 밴율 | 내가 없는 판 밴율 | 순수 견제 |
|---|---|---|---|---|
| 맛동산장인 유미 | 파이크 | 61% | 4% | **+57%p** |
| 아 리 | 니코 | — | — | +73%p |
| 용조련사 | 트린다미어 | — | — | +60%p (단 본인 승률 35%) |
| 우 거 | 제드 | — | — | +45%p (본인 승률 87%) |

> 파이크는 유미가 없는 판에선 4%만 밴되는데, 유미가 끼면 61% 밴된다.
> 즉 "파이크가 센 챔프라서"가 아니라 **"유미가 파이크를 해서"** 밴하는 것이다.

---

## 2. 계산 방법 (★ 이 정의를 그대로 지킬 것)

선수 P, 챔프 C에 대해:

```
ban_present = (P가 참가 & 상대 진영이 C를 밴한 게임 수) / (P가 참가 & 밴이 있는 게임 수)
ban_absent  = (P 미참가 & 누구든 C를 밴한 게임 수 × 0.5) / (P 미참가 & 밴이 있는 게임 수)
targeted    = ban_present − ban_absent          ← 이게 "견제 압력"
z           = targeted / sqrt(p(1-p)/n 합)      ← 유의성. z > 2 면 뚜렷
steal_risk  = C의 클랜 전체 픽률(%)              ← 노밴 선언 시 상대가 뺏어갈 위험
```

**왜 `ban_absent`에 0.5를 곱하나**: `ban_present`는 *상대 한 팀*의 밴만 세고,
`ban_absent`는 *양 팀 전체* 밴을 세므로 스케일을 맞추기 위함. 빼먹으면 견제가 과소평가된다.

**★ 절대 밴율만 쓰면 안 된다.** "파이크 밴율 61%"만 보면 파이크가 원래 인기 밴인지
그 사람 때문인지 구분이 안 된다. 반드시 **present − absent 차이**를 써야 교란이 제거된다.
(초기 버전이 이 실수를 했고, 통제를 넣자 순위가 크게 바뀌었다.)

---

## 3. 검증 근거 (이미 완료됨 — 다시 안 해도 됨)

이 지표는 다른 후보들을 전부 탈락시키고 살아남은 유일한 것이다.

| 후보 지표 | 검증 | 결과 |
|---|---|---|
| 세션 N번째 판(웜업/피로) | 클랜 2,470판 | ❌ 1~7판째 전부 49~51% |
| 첫 판 패배 → 틸트 | 클랜 1,657판 | ❌ 효과 없음 |
| 게임 길이 | 클랜 전체 | ❌ 구조상 전부 50% |
| 궁합/천적 | 실력보정 로지스틱 | ❌ 보정 후 소멸(max z=1.3) |
| 승리기여 레이팅 | 반분 재현성 | ❌ 홀짝 r=0.53인데 **기간 넘으면 r=0.01** |
| 기복(경기력 편차) | 반분 재현성 | ❌ r=0.148 |
| **견제 압력** | 반분 재현성 + 교란통제 | ✅ **r=0.650**, 73명 중 46명 z>2 |

즉 **전반기에 견제받던 사람은 후반기에도 견제받는다**(r=0.65). 안정적 성향이라 노출해도 된다.

부수 발견: **진영 편향 실재** — 블루 51.9% vs 레드 47.3% (4,886판, p=0.007).
개인 지표는 아니지만 팀 배정 참고용으로 쓸 수 있다.

---

## 4. 데이터 소스

- 시트 ID: `10j2QBdXiyL0_UGKLMDcndieXD7jeMGxVHqH3nj6gJnU`
- 탭: `CLASSIC_NORMAL` (19열)
- 필요한 열: `게임ID(A)`, `소환사명(C)`, `진영(E)`, `챔피언(G)`, `밴(H)`, `결과(I)`
- 공개 gviz로 읽으면 **서비스계정 할당량을 안 쓴다**(웹이 이미 그 방식).

```
https://docs.google.com/spreadsheets/d/{SHEET_ID}/gviz/tq?tqx=out:json&sheet=CLASSIC_NORMAL&headers=1&tq={쿼리}
```

⚠️ **`headers=1` 필수** — 없으면 gviz가 헤더를 잘못 잡아 첫 열 라벨에 전체 데이터가 뭉쳐 들어온다(기존에 겪은 함정).

---

## 5. 준비된 스크립트

- `tools/noban_metric.py` — 전 선수 견제 압력 + 노밴 추천 계산 → JSON 출력
  - 실행: `py tools/noban_metric.py noban.json`
  - 현재 결과: 74명 분석, 53명 추천 대상 있음, 21명 없음
- `tools/ban_targeted.py` — 교란 통제 버전 단독 검증용
- `tools/noban.sample.json` — 실제 출력 샘플 (구조 참고용)

출력 스키마:

```json
{
  "맛동산장인  유미#Teana": {
    "best": {"champ":"파이크","games":24,"wr":46,"ban_present":61,
             "ban_absent":4,"targeted":57,"z":9.9,"steal_risk":9},
    "champs": [ ...동일 구조 상위 6개... ],
    "note": null
  }
}
```
`best`가 `null`이면 뚜렷한 견제 대상이 없다는 뜻이고 `note`에 사유가 들어간다.

---

## 6. 웹 적용 시 결정할 것

### 방식 A — 웹에서 직접 계산 (권장)
`index.html`이 이미 `CLASSIC_NORMAL`을 gviz로 읽고 있다면 밴·진영 열만 추가로 파싱해서
JS에서 계산한다. 시트 쓰기가 없어 파이프라인이 단순하다. 계산량은 5천 행 수준이라 가볍다.

### 방식 B — 시트에 미리 계산해 적재
`noban_metric.py` 결과를 새 탭(예: `BAN_PRESSURE`)에 넣고 웹은 읽기만 한다.
주간평 파이프라인과 같이 돌리면 되지만, 갱신 주기가 주 1회로 묶인다.

### 표시 위치 제안
프로필 카드의 AI선생 주간평 블록 근처. 기존 패턴 참고:
- `index.html:1380~1407` — `loadAiEval()` / `AI_EVAL` / `aiEvalOf()`
- `index.html:3118` 부근 — 프로필 카드 렌더 (`aiEvalHtml`)

문구 예시:
> 🎯 **견제 지수** — 상대가 당신의 **파이크**를 밴하는 비율
> 당신이 낀 판 **61%** vs 없는 판 **4%** → 클랜 4위

---

## 7. ★ 함정 / 주의사항

1. **중복 행** — 시트에 `(게임ID, 소환사명)` 중복이 존재한다(12게임, 150행).
   반드시 유니크 처리 후 계산할 것. 안 하면 밴율이 왜곡된다.
2. **밴 없는 게임** — 488게임 중 **125게임이 밴 0개**다(`밴 없음` 또는 공란).
   분모에서 제외해야 한다. 포함하면 전원 밴율이 희석된다.
3. **인원 ≠ 10 게임** — 15건 존재(2·3·6명짜리 불완전 기록). 6인 미만은 버릴 것.
4. **노밴 로컬룰로 인한 과소추정** ⚠️
   클랜엔 **노밴** 룰이 있다: 게임 시작 전 팀당 1개, 선언하면 상대가 그 챔프를
   1페이즈에 밴 못 한다(2페이즈엔 가능, 상대가 픽하는 건 못 막음).
   → 팀이 노밴으로 지켜준 판은 **"밴 안 됨"으로 집계**되어 견제가 실제보다 낮게 나온다.
   → 게다가 **잘하는 사람일수록 보호받으므로 편향이 지표와 같은 방향**이다.
   → 노밴은 시트에 **전혀 기록되지 않는다**(밴 총수가 10개로 유지되어 흔적이 없음).
     사장님이 봇 입력 추가는 원하지 않으므로, **"현재 값은 하한선"**으로만 해석할 것.
5. **해석 주의** — 밴은 "무섭다"뿐 아니라 **"상대하기 짜증난다"**에도 걸린다
   (예: 용조련사의 트린다미어는 견제 2위인데 본인 승률 35%). 실력 훈장으로만 쓰면 안 된다.
6. **닉변/부계** — 표시할 때 반드시 기존 `aliasLookup` 경로를 태울 것.
   과거에 닉네임 키 직접 조회로 사고가 난 적 있다(새벽문앞→일단즐겨).
   부계 통합은 `LINK_ACCOUNT` 탭.

---

## 8. 배포 시

- 웹 배포는 **사장님이 "릴리스해"라고 명시할 때만** 한다(상시 원칙).
- 배포할 때 `index.html`의 **BUILD_ID sed 치환 필수**(기존 배포 절차).

---

## 9. 참고 — 함께 만들어진 것 (이 작업 범위 밖)

주간평(AI_EVAL) 파이프라인도 같은 세션에서 개편되어 이미 시트에 반영 완료:
- 5판 미만 게이트 + 안내문, 30일 평가 보존, 챔프×포지션 교차집계
- 지표 `od`(오브젝트 딜) → `op`(오브젝트 처치 관여율 %) 변경 반영
- 포지션 백분위는 **`bypos`에만** 존재(전체 평균을 주포지션 코호트와 비교하면 왜곡)
- AI_EVAL 탭은 현재 5열(`닉네임·평가·갱신일·상태·직전평가일`),
  웹은 앞 3열만 라벨로 찾으므로 뒤 2열은 무해

노밴 추천 문구는 다음 주간평부터 자동 포함될 예정이다(현재 시트 117명분엔 없음).
