# 발송할 패치노트 (patch-note 워크플로의 notes_file 로 지정해 사용)
#
# 이 파일에 적고 커밋한 내용을 그대로 발송한다. 워크플로 입력칸에 직접 타이핑하지 않는 이유는
# 그 경로에서 한글이 깨지는 사고가 반복됐기 때문이다(2026-07-30, 2026-07-31 재발).
# 머리말은 '## 분석기' / '## 웹' / '## 봇' 세 가지. 빈 칸은 발송에서 자동 제외된다.

## 봇
💸 대출 상환이 자동으로 바뀌었어요
빚이 있으면 **버는 포인트의 절반**이 그 자리에서 빠지고, 매일 새벽 **남은 빚의 20%**도 걷어갑니다
`/상환` 으로 미리 갚는 건 그대로 되고, 먼저 갚을수록 이자가 덜 붙어요
🚫 걷어갈 포인트가 없으면 **잔액이 마이너스로 내려가고 신용불량**이 됩니다
신용불량이 되면 새로 빌릴 수 없고, 버는 포인트가 전부 구멍을 메우는 데 들어가요 — 0P 로 올라오면 자동으로 풀립니다
-# 빌리고 잠수하면 더 손해입니다. 내 상태는 `/포인트` 에서 확인하세요
