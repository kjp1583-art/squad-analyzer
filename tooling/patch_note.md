# 발송할 패치노트 (patch-note 워크플로의 notes_file 로 지정해 사용)
#
# 이 파일에 적고 커밋한 내용을 그대로 발송한다. 워크플로 입력칸에 직접 타이핑하지 않는 이유는
# 그 경로에서 한글이 깨지는 사고가 반복됐기 때문이다(2026-07-30, 2026-07-31 재발).
# 머리말은 '## 분석기' / '## 웹' / '## 봇' 세 가지. 빈 칸은 발송에서 자동 제외된다.

## 웹
🎪 **내전 자동매칭**이 생겼습니다 — squad.gg 의 `내전모집` 탭에서 입장
롤아이디 적고 참가 신청하면, **10명이 차는 순간 시스템이 알아서 5:5 팀을 짜서** 디스코드에 로스터를 올립니다
모드는 두 가지 — **포지션 지정**(주·부 자리로만 매칭) / **자동 조율**(시스템이 알아서 배치)
팀은 내부티어를 보고 전력이 비슷하게 갈라줍니다
게임이 끝나면(전적이 기록되면) 참가자들은 **자동으로 다음 대기열에 다시** 들어갑니다 — 계속 돌리다가 쉬고 싶으면 **막판** 버튼만 누르면 그 판을 끝으로 빠져요
대기열·진행 중 로스터는 실시간으로 보입니다

## 봇
🏆 LCK 승부예측 일정이 다시 잡힙니다 (일정 제공처가 문을 닫아서 갈아탔어요)
